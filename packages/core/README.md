# @goodies-beacon/core

Domain types, Zod schemas, the database schema and the matching-pipeline logic shared by every other package. Nothing in here talks to the network or a specific AI provider; it is the vocabulary the rest of Goodies Beacon speaks.
