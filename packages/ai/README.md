# @goodies-beacon/ai

The three AI roles (interviewer, prefilter, reviewer), their versioned prompts, a provider factory over the Vercel AI SDK so providers are swapped by configuration, and the cost ledger. Every model call in Goodies Beacon goes through this package.
