/**
 * Template source adapter.
 *
 * P0-01 scaffold: a placeholder shape so the workspace compiles. The real `SourceAdapter`
 * interface from ARCHITECTURE.md §5 replaces this in P1-03.
 */
export const adapter = {
  id: '_template',
  displayName: 'Template (fixture data)',
  requiresBrowser: false,
} as const;
