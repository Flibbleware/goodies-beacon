# @goodies-beacon/source-ebay

The eBay Browse API adapter: one search request per marketplace on a plan's region, `getItem` for enrichment, and a health check that reports the daily quota. Written against the fixtures S1-01 recorded, which are in `fixtures/` and replayed by the tests — see `docs/ADAPTERS.md`.
