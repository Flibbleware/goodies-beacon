# @goodies-beacon/web

The React web UI (Vite, React 19). Pages: dashboard, wanted items, interview chat, candidates and verdicts, grading scales, settings, costs. Built to static files and served by `@goodies-beacon/api` in production.
