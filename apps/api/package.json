{
  "name": "@goodies-beacon/api",
  "version": "0.0.0",
  "private": true,
  "description": "Hono HTTP server: authentication, JSON API, interview chat streaming, and static serving of the built web app.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/main.ts",
    "build": "tsc -b",
    "start": "node dist/main.js",
    "test": "vitest run --root ../.. --project @goodies-beacon/api"
  },
  "dependencies": {
    "@goodies-beacon/core": "workspace:*",
    "@hono/node-server": "2.1.1",
    "hono": "4.13.7"
  },
  "devDependencies": {
    "tsx": "4.23.13"
  }
}
